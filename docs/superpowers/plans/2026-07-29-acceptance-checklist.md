# BingX 下单链路 验收清单

自动化验证到此为止。以下步骤需要人工执行，其中合约真单**必须由项目所有者亲自操作**。

## A. 自动化（已完成才勾）

- [ ] `npm test` 全绿（trading 纯函数层）
- [ ] `npm run build` 无错误
- [ ] `node scripts/verify-order-dry-run.mjs` 9/9 通过（需要在本地/CI 之外的环境提供真实 `BINGX_API_KEY`/`BINGX_SECRET`；本任务未在本环境执行，因为没有可用凭证）
- [ ] 三语 key 结构校验脚本无 `missing in` 输出

## ⚠️ 当前阻塞项（必须先解决才能继续走 B/C/D）

- [ ] **`supabase/migrations/020_trading_limits.sql` 尚未由项目所有者执行。** 截至本任务完成时，该迁移仍未在任何环境跑过。Task 11/12/13/14/15/17/18/24 都因此无法对真实数据库做端到端验证——本清单中所有涉及 `orders` 表落库、风控限额读取/写入的条目（B 部分、D 部分）在迁移执行前都无法完整验证，请勿跳过这一步就去验收后续条目。

## B. 服务端权威性（浏览器 Console，不产生真单）

- [ ] 金额 0.5 USDT 下单 → 返回 `BELOW_MIN_NOTIONAL` 或 `ZERO_AFTER_ROUNDING`，未打交易所
- [ ] 配置全局单笔限额 500 后，下 900 USDT → 返回 `NOTIONAL_TOO_LARGE`
- [ ] 上一条在 `orders` 表留下 `risk_rejected = true` 的记录
- [ ] **价格防篡改（Task 12b 修复的安全洞验证，不是简单的“下单是否成功”）**：在浏览器 Console 里构造请求，把 body 里的 `price`/参考价字段改成远低于市场价的假值（例如实际现价的 1/10），确认服务端风控计算仓位名义额时使用的是服务端自己拉取的价格，而不是这个客户端谎报的价格——即用假低价不能让同样的 USDT 金额换算出更大的仓位/绕开限额。预期：无论客户端传什么价格，`NOTIONAL_TOO_LARGE`（或对应放行）判断结果都不变。
- [ ] `priceRatePercent: 150` → 返回 `INVALID_CALLBACK_RATE`，未打交易所
- [ ] 未绑 Key 的账号下单 → 返回 `NO_API_KEY` 而非 500

## C. 界面（`npm run dev`）

- [ ] 现货点「50%」填入余额一半，不是字符串 `50`
- [ ] 现货与合约都显示可用余额
- [ ] 合约输入 100 USDT，预览显示「所需保证金 = 100 ÷ 杠杆」，不是 100
- [ ] 合约杠杆按钮点击后有 pending 态；设一个超上限值会显示可读错误而非静默成功
- [ ] 合约点下单先弹确认框，框内含杠杆 / 所需保证金 / 预估强平价
- [ ] 杠杆 > 20x 时确认框出现高杠杆警示
- [ ] **杠杆确认竞态（Task 20/21 修复的 Critical 缺陷之一）**：在打开合约确认框前，快速连续切换 市场（现货↔合约）与 方向（多↔空）多次，再提交订单；确认最终发出的杠杆值与确认框里展示、用户已确认的杠杆值一致，不会因为 market-switch/direction-flip 竞态而把未经确认的杠杆带到真实下单请求里
- [ ] **TP/SL 在现货/模拟盘下应真正不存在（Task 20/21 修复的另一 Critical 缺陷）**：切到现货或 paper 模式，确认 TP/SL 输入控件是真的从 DOM 里移除/不渲染，而不是仅仅通过 CSS 隐藏或 disabled——用浏览器 DevTools 检查元素树确认；同时确认此时提交的下单请求 body 里不含 stopLoss/takeProfit 字段
- [ ] API Key 页显示真实前4后4掩码，现货/合约权限分别标记
- [ ] API Key 添加弹窗显示 IP 白名单警告
- [ ] 三语切换下单表单与确认框均无 raw key 泄漏

## D. 真实资金（需人工，按顺序；需先确认迁移 020 已执行）

- [ ] **现货小额真单**：BTC-USDT 市价买入约 6 USDT（略高于最小 5），确认成交
- [ ] 该笔出现在 `/orders` 页面（验证落库）
- [ ] **合约真单（项目所有者亲自执行）**：
  - [ ] 名义额 5–10 USDT、杠杆 **1x**、市价开多
  - [ ] 确认框显示的「预计数量」与交易所实际成交数量一致 ← **这是 A1 缺陷修复的最终证据**
  - [ ] 仓位出现在 Positions 面板
  - [ ] 点「平仓」成功平掉（验证 positionId 平仓路径）
  - [ ] 两笔都出现在 `/orders` 页面

## E. 已知未覆盖 / 已知遗留缺口

- **VST（BingX 模拟交易环境）未接入本次重构** —— 这是设计规格里的既定决定，不是遗漏。上面 D 部分的合约真单验证没有 VST 兜底，只能由人工用真实资金验证一次；不要假设有任何形式的 VST 覆盖存在。
- `/api/bingx/account/balance` 与 `/api/bingx/futures/positions` 这两个只读路由仍然用未排序的 `.limit(1)` 方式取 API Key（Task 18 发现），没有像下单路由那样按 `is_primary` 排序取主密钥。这是本次重构刻意界定在范围外（out of scope）的已知缺口，多 Key 用户在这两个只读接口上可能读到非主密钥的余额/仓位数据，需要单独立项修复。
- 内存限流在 Vercel 多实例下无法跨实例生效；真正护栏是服务端限额
- 预估强平价为简化公式，未计入维持保证金率与手续费，仅作量级提示
- `supabase/migrations/020_trading_limits.sql` 未执行（见上方“当前阻塞项”），意味着风控限额、订单落库等相关的自动化和人工验证目前都只能停留在代码审查层面，未经真实数据库验证
